services:
  backend:
    build: ./backend
    container_name: mysql-agentic-copilot-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
    restart: unless-stopped
